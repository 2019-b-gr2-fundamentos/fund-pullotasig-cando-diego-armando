function imprimirElementos(arregloNumeros, n) {
    if (arregloNumeros === void 0) { arregloNumeros = [1, 2, 3]; }
    if (n == 3) {
        console.log('Se termino');
    }
    else {
        var arregloNumeros_1 = [1, 2, 3];
        console.log(elemento);
        var nuevoNumeroDeVeces = n + 1;
        imprimirElementos(elemento, nuevoNumeroDeVeces);
    }
}
var arregloNumeros = [1, 2, 3];
function main() {
    imprimirElementos(arregloNumeros.toString(), 2);
}
main();
//necesito cambiar mensaje por cada uno de los ellementos por lo tanto tengo que poder acseder a cada uno de ellonumero de veces lo doy yo porque me dan el arreglo
